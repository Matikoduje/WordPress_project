<?php
/**
 * Theme hooks.
 * @author  	 DeoThemes
 * @copyright  (c) Copyright by DeoThemes
 * @link       https://deothemes.com
 * @package 	 Blockst
 * @since 		 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function blockst_head_top() {
	do_action( 'blockst_head_top' );
}

/**
 * Head bottom
 */
function blockst_head_bottom() {
	do_action( 'blockst_head_bottom' );
}

/**
 * Header before
 */
function blockst_header_before() {
	do_action( 'blockst_header_before' );
}

/**
 * Site Header
 */
function blockst_header() {
	do_action( 'blockst_header' );
}

/**
 * Header after
 */
function blockst_header_after() {
	do_action( 'blockst_header_after' );
}

/**
 * Masthead
 */
function blockst_masthead() {
	do_action( 'blockst_masthead' );
}

/**
 * Menu before
 */
function blockst_menu_before() {
	do_action( 'blockst_menu_before' );
}

/**
 * Menu after
 */
function blockst_menu_after() {
	do_action( 'blockst_menu_after' );
}

/**
 * Last menu item after
 */
function blockst_last_menu_item_after() {
	do_action( 'blockst_last_menu_item_after' );
}

/**
 * Body top
 */
function blockst_body_top() {
	do_action( 'blockst_body_top' );
}

/**
 * Body bottom
 */
function blockst_body_bottom() {
	do_action( 'blockst_body_bottom' );
}

/**
 * Primary content top
 */
function blockst_primary_content_top() {
	do_action( 'blockst_primary_content_top' );
}

/**
 * Primary content before
 */
function blockst_primary_content_before() {
	do_action( 'blockst_primary_content_before' );
}

/**
 * Primary content query
 */
function blockst_primary_content_query() {
	do_action( 'blockst_primary_content_query' );
}

/**
 * Primary content after
 */
function blockst_primary_content_after() {
	do_action( 'blockst_primary_content_after' );
}

/**
 * Primary content bottom
 */
function blockst_primary_content_bottom() {
	do_action( 'blockst_primary_content_bottom' );
}

/**
 * Comments before
 */
function blockst_comments_before() {
	do_action( 'blockst_comments_before' );
}

/**
 * Comments
 */
function blockst_comments() {
	do_action( 'blockst_comments' );
}

/**
 * Comments after
 */
function blockst_comments_after() {
	do_action( 'blockst_comments_after' );
}

/**
 * Entry section before
 */
function blockst_entry_section_before() {
	do_action( 'blockst_entry_section_before' );
}


/**
 * Content before
 */
function blockst_content_before() {
	do_action( 'blockst_content_before' );
}

/**
 * Content top
 */
function blockst_content_top() {
	do_action( 'blockst_content_top' );
}

/**
 * Content bottom
 */
function blockst_content_bottom() {
	do_action( 'blockst_content_bottom' );
}

/**
 * Content after
 */
function blockst_content_after() {
	do_action( 'blockst_content_after' );
}


/**
 * Entry featured image before
 */
function blockst_entry_featured_image_before() {
	do_action( 'blockst_entry_featured_image_before' );
}

/**
 * Entry featured image
 */
function blockst_entry_featured_image() {
	do_action( 'blockst_entry_featured_image' );
}

/**
 * Entry featured image after
 */
function blockst_entry_featured_image_after() {
	do_action( 'blockst_entry_featured_image_after' );
}

/**
 * Entry content top
 */
function blockst_entry_content_top() {
	do_action( 'blockst_entry_content_top' );
}

/**
 * Entry content bottom
 */
function blockst_entry_content_bottom() {
	do_action( 'blockst_entry_content_bottom' );
}

/**
 * Page title before
 */
function blockst_page_title_before() {
	do_action( 'blockst_page_title_before' );
}

/**
 * Page title after
 */
function blockst_page_title_after() {
	do_action( 'blockst_page_title_after' );
}

/**
 * Sidebar before
 */
function blockst_sidebar_before() {
	do_action( 'blockst_sidebar_before' );
}

/**
 * Sidebar after
 */
function blockst_sidebar_after() {
	do_action( 'blockst_sidebar_after' );
}

/**
 * Footer before
 */
function blockst_footer_before() {
	do_action( 'blockst_footer_before' );
}

/**
 * Footer
 */
function blockst_footer() {
	do_action( 'blockst_footer' );
}

/**
 * Footer after
 */
function blockst_footer_after() {
	do_action( 'blockst_footer_after' );
}